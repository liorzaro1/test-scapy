from scapy.all import *

packets = rdpcap('nisoy.pcap')
print(packets)

def ip_check(i):
    if packets[IP] = 142
    for i in range(0,len(packets))
    packets = IP(dst='nisoy.pcap')/ICMP()
    pkt = packets[i]
    pkt.show()
    sr1(packets)